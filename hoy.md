git add . ---"prepara"

git commit ---"guarda en el pc"

git push ---"aplica en el github"

Una rama local no se ve en GitHub hasta git push -u origin nombre.

flex-wrap deja bajar de línea si no caben.

En CSS, la clase (.servicio-card) gana sobre un div genérico.

Un div dentro de ul, o una etiqueta sin >, rompe el diseño.

